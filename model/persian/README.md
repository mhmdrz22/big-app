# Persian Model Directory

Place `stress_model_persian.joblib` and `metrics_persian.json` here after running:

```bash
python train_persian.py --train data/persian-train.csv --test data/persian-test.csv --output model/persian
```

Until then, Persian input falls back to `support_only` mode.
